# Production environment variables — used by `npm run build`
# Set this to your Cloudflare Worker URL after deploying:
#   cd worker && npx wrangler deploy
# Then copy the URL shown (e.g. https://estimation-proxy.YOUR-SUBDOMAIN.workers.dev)
VITE_WORKER_URL=https://estimation-proxy.icaestapp.workers.dev
