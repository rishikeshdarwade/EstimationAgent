{
  "name": "estimation-proxy-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker proxy for the Estimation Agent API",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
