name = "estimation-proxy"
main = "index.js"
compatibility_date = "2024-01-01"

# The Worker is deployed to:
# https://estimation-proxy.<YOUR-SUBDOMAIN>.workers.dev
#
# After deploying, set your API key as a secret (NOT in this file):
#   npx wrangler secret put AGENT_API_KEY
# Then paste your API key when prompted.
